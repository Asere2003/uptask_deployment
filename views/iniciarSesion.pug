<!DOCTYPE html>
html(lang="en")
    head
        include partials/header.pug  
    body.login
        .contenedor-formulario
            h1 Inicia Sesion en UpTask

            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class= `${categoria}`)
                                p= error

            form#furmulario.caja.caja-login(method="POST" action="/iniciar-sesion")
                .campo
                    label(for="email") Email
                    input(type="email" name="email" placeholder="Tu Email" value= email )
                .campo
                    label(for="password") Password
                    input(type="password" name="password" placeholder="Tu Password" value= password )
                .campo.enviar
                    input.boton(type="submit" value="Iniciar sesión")
                .campo.acciones
                    a(href="/crear-cuenta") ¿Aún no tines cuenta? Crea una gratis
                    a(href="/reestablecer") ¿Olvidaste tu contraseña?